---
description: docs を実装に合わせ、置き場所と索引を整え、伸びたものを削る
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, Task
argument-hint: "[ファイル名 | --since <git-ref>] [--read-only]"
---

**「書いてあるから正しい」を一度も使わない。**docs は実装より先に古びる。
ここは、ずれを見つけて直すところ。

## 1. 対象を決める

```bash
K=$(ls ~/.claude/plugins/cache/*/claude-keeper/*/scripts/k.py 2>/dev/null | head -1)
[ -n "$K" ] && python3 "$K" check --only docs || echo "claude-keeper 未導入。機械の候補なしで進める"
```

| 引数 | 対象 |
|---|---|
| なし | 未コミットの差分に関わる文書 **+ `check --only docs` が出した指摘ぜんぶ** |
| ファイル名 | それだけ |
| `--since <ref>` | その参照からの差分 |
| `--renew` | **docs 全体を棚卸しして構成を引き直す。**流れが変わる(3 節) |
| `--read-only` | 読んで報告するだけ。直さない |

**「差分が無いから対象が無い」で終わらせない。**差分に何も無くても、機械が出した
指摘(散らばり・伸びた節・置き場所・索引)は対象に入る。ここを差分だけに絞ると、
**動きの止まっている文書の散らかりは永久に出てこない。**

### 初めて回すとき / 半年ぶりに回すとき

差分ではなく**棚卸しをする。**機械の指摘が 0 件でも、これだけは自分で見る:

1. `docs.layout` の `role` を読み、**各文書を開いて、その役割どおりの中身か**
   確かめる。役割から外れた節は、正しい文書へ出す候補
2. `CLAUDE.md` を全文読む。**そこにある必要があるか**を節ごとに問う
   (skill `claude-md`)。設計・仕様・デザイン規定は `docs/` の仕事
3. 出す先が `layout` に無ければ、**`layout` に足してから**出す

**手が付けられていない文書がいちばん危ない**と思ったら `--renew` へ。

## 2. 読ませる

`.claude/agents/docs-auditor.md` へ投げる(会話の経緯は渡さない)。
役が置かれていなければ skill `flat-view` を読んで自分で読む。

**読む順番が肝心** — docs を先に読み、実装を見る前に「これが本当なら何ができる
はずか」を書き出す。逆順だと、docs の記述を実装で補って読んでしまい、
**足りない記述と間違った記述が両方とも見えなくなる。**

| 見るもの | 判定 |
|---|---|
| docs にあり、実装にある | 合っている |
| docs にあり、実装に無い | **嘘。いちばん高くつく** |
| docs に無く、実装にある | 抜け |
| docs にあるが、動かすと違う | 古い。**動かして確かめる** |

{{このプロジェクトで特に食い違いやすいところ。2〜4 行}}

## 3. 直す

**書き換える前に一覧を見せて承認を取る。**種類ごとに手順が違う。

### 実装に合わせる

「最新化して」を頼み忘れたぶんを埋める。

- **変更の中身を実際に読んでから書く。**ファイル名だけで docs を書き換えない
- **最小差分。**周りの文章は整えない(整えると差分が読めなくなる)
- 実装履歴は記録(`archive`)にだけ書き、仕様書には**結論だけ**残す
- 済んだ TODO は記録へ移し、索引も直す
- 最後に**何を書かなかったか**とその理由を報告する

### 置き場所と索引を整える

中身の書き換えはしない。移動と索引だけ。

1. 索引から辿れない / 規約にない場所 / リンク切れ / 内容が重なっているものを拾う
2. **「移動 / 統合 / 削除 / 残す」の一覧を先に出して承認を取る**
3. `git mv` で動かす(履歴を切らない)
4. 索引を書き直し、**リンク切れを全部直す**
5. 規約の `docs.layout` を実態に合わせる。**実在しない場所の行は消す** —
   残すと `guard` がそこへの書き込みを許し、`watch` が未更新だと言い続ける

### 散らばりと二重管理を寄せる

`check` の「同じ話が複数の文書にある」「同じ文が複数の文書にある」
「CLAUDE.md の伸びた節」に対して。**機械が出すのは場所だけ。**

**先に `.claude/agents/duplication-auditor.md` へ投げる**(会話の経緯は渡さない)。
**機械が拾うのは字面がそろっているものだけで、言い換えて写したものは出ない。**
役が置かれていなければ skill `single-source` を読んで自分で突き合わせる。

寄せるかどうかは skill `single-source` の判定で決める —
**字面ではなく、変わるときに一緒に変わるかで見る。**

| 読んでみたら | どうするか |
|---|---|
| 同じ事実が 2 か所以上にある | **寄せ先を 1 つ決める。**他は寄せ先を指す 1 行にする |
| 同じ事実だが内容が違う | **どちらが正か実物で確かめてから**寄せる。新しいほうを正としない |
| 片方だけ直しても両方正しいまま | **別の事実。触らない** |
| 見出しは同じだが別の話 | 見出しを分ける。`docs.overlap.ignore` に入れるのは最後の手 |
| CLAUDE.md の節が読み物になっている | `docs/` へ全文出し、CLAUDE.md には 1 行残す |
| 出す先が `layout` に無い | **`layout` に足してから**出す (デザイン規定なら `docs/design.md`) |

- 寄せ先は「その話が主役の文書」。仕様は `spec.md`、作りの理由は
  `architecture.md`、見た目の規定は `design.md`。**多いほうへ寄せない**
- **切り出すときは全文を移す。要約しない。**要約したぶんは失われる
- 移したあと、元の場所には**「どこへ行ったか」の 1 行**を必ず残す
- 出した先を索引に足す。**足し忘れると迷子が 1 つ増えるだけ**
- 寄せ終えたら `check --only docs` を回し直す。**残っていれば寄せ切れていない**

### 引き直す — docs 全体を棚卸しする (`--renew`)

ここまでは**決めた置き場所に対して揃える**手順。`--renew` は**置き場所の
決め方そのものを引き直す。**

狙いは**決まりものではない文書。**`README.md` / `CLAUDE.md` / `spec` /
`architecture` / `TODO` / `changelog` / `archive` は雛形どおりなので、
たいてい誰かが見ている。**危ないのはその外**で、そのプロジェクトのために
一度だけ作られ、そのまま誰も開いていない md。

#### 1. 全部並べる

```bash
K=$(ls ~/.claude/plugins/cache/*/claude-keeper/*/scripts/k.py 2>/dev/null | head -1)
[ -n "$K" ] && python3 "$K" check --only docs
git ls-files '*.md' | grep -v '^docs/archive/'
```

**`check` に出ないものも並べる。**規約に載っていて上限内で索引からも辿れる
文書は 1 件も出ないが、**中身が死んでいるかどうかは別の話。**

各ファイルに、最後に触られたのがいつかを付ける:

```bash
git log -1 --format='%ad %h' --date=short -- <パス>
```

#### 2. 1 つずつ問う

**決まりもの以外は全部開く。**拾い読みしない。4 問に答える:

| 問い | 答えられないなら |
|---|---|
| **誰が、どんなときに開くか** | 誰も開いていない。3 節へ |
| **まだ本当か** | 実装と照らす。嘘なら残さない |
| **ここが持ち主か** | 別の文書が持つべきなら寄せる (skill `single-source`) |
| **`layout` の `role` どおりか** | 役割から外れた節を出す |

**「あると便利そうだから残す」を使わない。**それで残った文書が、
半年後に「書いてあるから正しい」と読まれて事故になる。

#### 3. 仕分ける

**一覧を先に出して、1 件ずつ承認を取る。**

| 仕分け | どうするか |
|---|---|
| **残す** | `layout` に `role` を**書く**。役割を言えない文書は残さない |
| **寄せる** | 持ち主の文書へ全文移し、元は 1 行のリンクにする |
| **記録へ** | 過去の姿として意味があるなら `archive/` へ。**日付を付ける** |
| **消す** | 嘘 / 空 / 一度も参照されていない。**git に残るが、一覧は報告に書く** |

古いだけで消さない。**古い文書の正しい行き先は `archive/` で、削除ではない。**
消すのは**嘘になっているもの**だけ。

#### 4. 構成を引き直す

1. `docs.layout` を書き直す。**残した文書は全部載せ、`role` を 1 行ずつ書く**
2. `docs.limits` を実測から取り直す(`measure`)
3. `git mv` で動かす(履歴を切らない)
4. **索引 `docs/README.md` を書き直す。**分類は 3 節の仕分けをそのまま使う
5. **リンク切れを全部直す。**移した先を指しているものを grep する
6. `docs.watch` を見直す。**消えた文書を指している規則を消す**

#### 5. 数え直す

```bash
[ -n "$K" ] && python3 "$K" check --only docs
```

- 文書の件数 **before → after**
- 索引から辿れないものが 0 件か
- **`layout` に `role` の無い文書が 1 つも無いか**

### 長さと文体を直す

skill `docs-style`(禁止事項リスト)がそのまま作業指示。**削る前に仕分ける:**

| 種類 | 行き先 |
|---|---|
| いまの仕様・設計・手順 | **残す**(文体だけ直す) |
| 実装履歴・試したこと・没案・日付つきの経緯 | **記録へ移す**(消さない) |
| 空虚な前置き・重複した要約・一般論・コードの言い換え | **消す** |

**箇条書きが 5 個以上並んで各項目に説明が付いていたら表にする。**
これだけで 3 割減ることが多い。

移した結果、元のファイルから「なぜそうなっているか」が失われるなら、
**1 行に圧縮して残す。**

## 4. 記録 (archive) の扱い

記録は**短く保つものではなく、引けるようにするもの。**1800 行の 1 ファイルは
grep できるかぎり困らない。困るのは「どこに何があるか分からない」ほう。

- 記録に対しては「archive 送り」ではなく**「割りどき」**と言う
- 割るのは期間か版で。`git mv` で分け、**索引に 1 行足す**
- **中身は一切書き換えない。**記録を圧縮すると後から引く意味が無くなる
- **実験結果は記録へ送らない。**「この条件で回したらこうなった」は次と
  比べるためのもので、送ると比較対象を失う。送るのはその系統を追うのを
  やめたときだけで、そのときは**やめた理由を必ず書く**

## 5. 確かめる

- **消した情報が本当に不要か。**判断・罠・警告・動くコマンドを消していないか
- **リンク切れを作っていないか。**消した節に他から飛んでいないか grep する
- **同じ事実を書いている他の文書を直したか。**片方だけ直っている状態が
  いちばん見つけにくい
- 記録へ移したぶんが索引から辿れるか

## 6. 判断を残す

直さないと決めたものは `.claude/judgments.yml` へ(書式は `/code` の 5 節)。
`until` を書くと、その日を過ぎたときにもう一度言われる。

## 7. 報告

- ファイルごとに **「507 行 → 480 行」** の形で
- 記録へ移した項目の件数と行き先
- **消したもののうち、判断が微妙だったものを 2〜3 件**(戻せるように)
- **書かなかったこと**とその理由
- **寄せた話と寄せ先。**「デザイン規定を CLAUDE.md と spec.md から
  docs/design.md へ寄せた」の形で
- `--renew` なら:**文書の件数 before → after**、残す / 寄せる / 記録へ /
  消す の内訳、**消したものは 1 件ずつ理由つきで**、
  引き直した `layout` と、それぞれの `role`
